// Casing: camelCase!

var isAlive = true // Good!
var LastName = "smith" // Bad...
var first_name = "jack" // Bad..

// whitespacing: use it!

for(var i = 0; i < 10; i++){

}



// naming: 
    // use concise, logical, and meaningful names
    // make arrays plural
    // booleans usually ask questions
    // functions should be be somewhat desriptive


// braces
    // use indention to help aalign start and end of bracjets
    var person = {
        address: {
            
        }
    }

// tab / indenting:
// incorporate logic/code +1 indention within if/loops/functions

if (2 === 2){
    console.log("this is the correct place to indent")
console.log("this is NOT the correct place to indent")

    if(3 === 3) {
        console.log("Since this resides within a second if-statement")
    }

} 

// HTML Tags
    // all tags are lower case..

// comments - Two types:

    /*
    Multi-Line comments can be started and ended like this
    Alternatively, you can do it to batch of code using 
    'shift', 'option', and 'a'
    */

    // Single line casing... You can do this to a code selection using "command" and "/"

// unix casing with folders and directory names:
    // e.g., 'Silly CIA' - BAD
    // e.g., 'silly-cia' - GOOD!



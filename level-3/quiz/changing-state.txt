1. You have 2 options for what you can pass in to a
   state setter function (e.g. `setCount`). What are they?
A value that will replace the state or a callback function that will determine the new value of state

2. When would you want to pass the first option (from answer
   above) to the state setter function?
If the previous state is NOT required in determining the new state.


3. When would you want to pass the second option (from answer
   above) to the state setter function?
If the previous state is required in determining the new state.